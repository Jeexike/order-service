apiVersion: 1

providers:
  - name: order-service
    orgId: 1
    folder: Order Service
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards